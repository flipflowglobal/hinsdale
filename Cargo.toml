[package]
name        = "hinsdale"
version     = "1.0.0"
edition     = "2021"
description = "EVM bytecode decompiler — Rust core, Python FFI"
authors     = ["Aureon/DL System"]

[lib]
name    = "hinsdale"
crate-type = ["cdylib", "rlib"]
# cdylib  → libhinsdale.so  (linked by Cython _hinsdale.so)
# rlib    → libhinsdale.rlib (used by hinsdale-cli binary)

[[bin]]
name = "hinsdale-cli"
path = "src/main.rs"

[dependencies]
# Core
serde       = { version = "1", features = ["derive"] }
serde_json  = "1"
hex         = "0.4"
rayon       = "1.8"          # parallel analysis — critical for long bytecode
indexmap    = "2"            # ordered maps for stable output
smallvec    = "1"            # stack-local vecs for opcode args
rustc-hash  = "1"            # fast non-crypto hashing

# Python FFI
pyo3        = { version = "0.21", features = ["extension-module"], optional = true }

[features]
default     = []
python      = ["pyo3"]

[profile.release]
opt-level   = 3
lto         = "fat"
codegen-units = 1
panic       = "abort"
strip       = "symbols"

[profile.release-termux]
inherits    = "release"
opt-level   = "z"            # size-optimize for Android RAM constraints
